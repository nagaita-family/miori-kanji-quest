# Miori Kanji Quest — 開発引き継ぎ

## 正式環境と開始手順
- 正式repo: `nagaita-family/miori-kanji-quest`、作業基準: 最新 `main`。
- 公開先: https://family.nagaita.jp/miori-kanji-quest/
- 毎回、最新main・本書・README・Actionsの状態と依頼内容を照合し、実施済み作業を繰り返さない。
- 通常の変更は必要最小限。学校学習・漢検島・既存の進捗を保護し、iPad + Apple Pencilを優先する。

## 今週の学校学習（2026-09-26登録）
- アプリ本体はv2.9.4。現在の学校パックは `data-packs.js` の `2026-09-26-p62`。
- 教材62ページ上半分「書く」①〜⑩のみ。下半分「ほかの言葉にチャレンジ！」は対象外。
- 順序: 客様、入学式、去年、二倍、毛筆、銀行、去る、筆、題名、横。
- 学校テスト表示は周辺の既習漢字もかなへ戻す。例: ③は `きょねんのあき。` と表示し、targetは `きょねん→去年` だけ。「あき」は解答対象にしない。
- 同様に⑤は `もうひつのしょもつ。`、⑨は `ほんのだいめい。`、⑩は `よこがきでじをかく。`。周辺かなには線・Canvasを付けない。
- ⑦ `きせつがさる。` の `さる` だけ波線で、`去る` を1つのCanvasへ書く。その他の今週targetは直線。
- 「今週の学校テスト」統合UXをそのまま使用: 線1本=1Canvas、全文書写なし、途中答え合わせ可、10問完了時だけ週別テスト結果保存、左右比較。
- `app-v236-school-test.js` / `app-v204-patch.js` / `app-v240-release.js` のcurrent判定はp58固定を廃止し、schoolTestを持つ現在packへ汎用化。
- p58 (`2026-09-21-p58`) は過去週として保持。漢字テスト14・85点を含む履歴と保存データは維持。
- 今後も `KANJI_PACKS` に追記し `CURRENT_KANJI_PACK_ID` を切り替える。既存パックを削除・上書きしない。

## 保存互換性
- 本番キーは `miori-kanji-quest-v10`。保存形式全体の移行・初期化はしない。
- 漢字単位の `save.stats`、語と送り仮名単位の `save.okuriStats`、週別の `save.printTestsV230[packId]` を維持。
- `save.completedStages` の旧数値キーはテスト13の履歴。新パックの完了は `kanjiStageCompletionKey(index)` により `packId:index` キーへ保存する。同じオブジェクト内なので、空島報酬の全カウンター合算と互換。旧キーを移行・重複加算しない。
- 漢検島は `save.kanken9V280` の独立進捗。学校の週追加では触らない。
- `test-mode-v261.js` をアプリの保存読込より先に読み込み、Parent Test Modeの保存分離を維持。
- 学習履歴はブラウザのlocalStorageで、端末間同期はない。更新のためにサイトデータを消さない。

## 安定性・PWA
- `app-v233-polish.js` は読み込み禁止。MutationObserverによるDOM更新ループを再導入しない。
- スクリプト読み込み順を維持。変更した配信JSのクエリ値を更新し、古いiPadキャッシュを避ける。
- `sw.js` は旧キャッシュの削除と登録解除用。古いオフラインキャッシュを復活させない。
- サブパス `/miori-kanji-quest/` 前提の相対パス、manifestの `start_url: './'` / `scope: './'` を維持。
- 漢検島間の往復のみ飛行演出。漢検島内の練習・テストから島への復帰は直接戻る。

## 検証・公開
- `.github/workflows/pages.yml` がmainへのpushで検証後にGitHub Pagesへ公開する。
- テストボタンは `app-v240-release.js` が現行プリントへ接続する。`app-v203-patch.js` の遅延処理で `data-v270wired` 接続済みボタンを上書きしない（旧テストが重なるため）。
- `app-v231-print-test-fix.js` はトップレベルconstの `QUEST_STAGES` を直接参照する（`window.QUEST_STAGES` ではない）。送り仮名ボタンを手書き欄の横へ配置するために必要。
- テストの拡大記入欄は `app-v231-print-test-fix.js` の組み替えで元の読み表示が隠れるため、`focusReadingCueV231` に問題の読み（送り仮名を含む）を明示する。小画面でも視認可能な位置に置く。
- `app-v235-paper-pdf.js` はテストの「A4プリント・PDF」から開く、今選択している10問の紙用プレビューを生成する。新しいウィンドウ内の「印刷・PDFとして保存」はブラウザ標準の印刷画面を開く。現行の印刷版はA4横1ページ、①〜⑩を右から左へ並べる縦書き。各問に仕切りのない19×48mm長方形を1つ設ける。送り仮名のある①⑤⑨⑩は読み全体を赤い波線で示し、「かな」専用枠を付けない。⑤の「こむ」は印刷済み。正答・画面で書いた筆跡・採点結果は載せず、保存データは変更しない。iPadでは印刷画面の共有からPDF保存する。
- `node --check`、既存の全 `*.cjs`、追加した `node weekly-p58-test.cjs` を実行する。
- p58テストは10問と順序、⑤の採点、旧週・クリア回数・テスト履歴・漢検島の保存保護を検証する。手書き字形の認識精度そのものはこの単体テストの範囲外。
- 古いv2.8.6/v2.9.3テストの版番号と飛行テスト用DOM/APIを現行v2.9.4に整合させている。漢検島本体は変更していない。
- 実ブラウザ回帰は `node tools/weekly-p58-browser.cjs`（PlaywrightとChromiumが必要）。`CHROMIUM_EXECUTABLE` でブラウザを指定可。KanjiVGのSVGを `KANJI_SVG_DIR` に置けば外部通信なしで手書き・消去・採点・画面遷移を確認できる。
- Actions成功と公開URLの実ファイル反映を確認するまで公開完了としない。iPad実機検証は自動テストと区別して報告する。

## 2026-09-21の検証記録
- 全JS/CJSの構文チェックと、ルートの自動テスト11本が成功。
- Chromiumで10問にKanjiVGの参照筆跡を入力し、⑤の送り仮名を誤った9点→正しい10点を確認。入力・消去、旧週から今週への復帰、旧テスト結果・漢検島データの保持を検証。
- 1024×768、744×1133、390×844で入力欄・操作ボタンの表示確認。520px以下だけ操作欄を下へ配置する。
- iPad / Apple Pencil実機は未確認。

## 2026-09-23の追加
- ユーザー報告のテスト記入画面の読み欠落を修正し、A4縦1ページの印刷・ブラウザPDF保存を追加。PDFのプレビューから印刷画面を開く2段階操作。
- `weekly-p58-test.cjs` で10問・⑤の「こむ」・正答非表示・A4印刷指定を検証。`tools/weekly-p58-browser.cjs` で拡大画面の読みを全問・3画面サイズで表示確認し、紙用プレビューとPDF出力、採点9→10点、旧学習履歴・漢検島を再確認する。
- A4プリントは2026-09-23に縦書きへ変更。`app-v235-paper-pdf.js` の上段①〜⑤・下段⑥〜⑩をそれぞれ右から左へ配置し、各問の問題文→19mm漢字マス（送り仮名は縦長ではなく短い別マス）→続きの文を上から下へ読ませる。読みはマスの右側、⑤の「こむ」はマスの外。A4縦1ページ、正答と保存データは従来どおり変更しない。`index.html` のクエリを更新し、旧iPadキャッシュを避ける。
- Chromiumで日本語フォントを埋めたPDFを描画して、右→左の順序、縦書き文字・ルビ、10問と⑤の紙面、A4縦1ページを視認している。フォントはQA専用で、アプリ本体には追加していない。
- 追加の紙面調整: A4横の1段に①〜⑩、全問1つの長方形記入欄。①⑤⑨⑩はiPad記入画面と同じ赤い波線付きの読み。ブラウザのA4横1ページPDFを日本語フォントで描画し、波線が枠や隣の問題にかからないことを確認する。印刷用だけを変更し、テスト入力・採点・保存形式は維持。

## 2026-09-25 学校テスト14の形式
- 返却済み「ぐんぐん58・漢字テスト14」は9/25・85点。テスト13（65点）に続けて `KANJI_TEST_HISTORY` へ記録のみ。間違えた箇所は断定せず、習熟度やおすすめへ反映しない。
- `data-packs.js` のp58パックへ `schoolTest` を追加。10問はかな文のsegment列で、直線 `straight` / 波線 `wavy` を持つ箇所だけがtarget。計20箇所。実答案を一次資料として転記。⑧は「すいえいきょうしつ」直線＋「かよう」波線、⑩は「こ」だけ直線＋「たすける」波線。⑤「もうしこむ」は「申・し・込・む」を順に記入し、漢字だけ自動判定、かなは正答比較で本人/保護者が確認。
- 旧 `stages` と普段のヒント・習熟度・XPは維持。`app-v236-school-test.js` の「学校テストれんしゅう」は1問を選び、複数targetを順に書く。「10問テスト」はヒントと正答を提出まで表示せず、targetをすべて書いてから一括採点。漢字の明確な判定は既存 `gradeKanjiStrokeV230` を使用。波線と判定が不確かな漢字は手書きと正答の比較確認へ進む。採点後の結果はtarget単位で表示する。
- 10問テスト結果は従来の `save.printTestsV230[packId]` を使用。1問練習は `save.schoolPracticeV236[packId]` に最終日時・問題番号のみ。どちらも `save.stats` / `save.okuriStats` / 漢検島を変更しない。旧週テストは従来のv230画面へ戻る。
- `app-v235-paper-pdf.js` は同じ `schoolTest` のかな文と線種をA4横へ描画し、複数targetを別々の仕切りのない長方形へ記入。直線は黒、波線は赤。正答・筆跡・採点結果は印刷しない。旧週の印刷は旧形式のまま。
- 回帰: `school-structure-test.cjs` と既存のルート `*.cjs`。実画面・PDF回帰用 `tools/school-test-browser.cjs` は1024×768と744×1133を確認する。iPad実機は未確認。現在のWork環境に残っていたChromium実行ファイルは起動時SIGSEGVとなったため、ブラウザ回帰の実施結果は別途記録すること。
- 公開サイトのブラウザ確認（通常のデスクトップ画面）: 10問のかな文と線種、⑧の直線4漢字入力から波線「かよう」への切替、1問練習の入口、A4プレビューを確認。A4画面は右→左10問・計20個の記入欄・直線11・波線9、正答なし。ローカルChromiumが起動できなかったため `tools/school-test-browser.cjs` は未実行で、1024×768・744×1133の自動操作と実際のPDFファイルのページ数確認は今回未完了。

## 2026-09-25 夜：実機からのUIフィードバック
- 1問練習・10問テストとも、かなの問題文を右、縦の記入欄を左へ配置。10問一覧の小さな欄も同じ左右関係。1問練習は従来どおり線のtargetのみ順番に書く。
- 10問テストは各問の**文全体**を右のかな問題文を見ながら左の大きな縦長Canvasに手書きする。線の箇所を漢字・送り仮名に直し、ほかのかなも含めて全文を書く。任意の改行を含む全文筆跡は漢字単字の形状判定で自動判定せず、10問を提出してから1問ずつ全文の正答と比べて本人/保護者が○・△・書き直しを選ぶ。結果は従来の週別 `save.printTestsV230` に記録、習熟度は変更しない。
- A4横は右のかな文（直線/赤波線）と左の全文用の長方形欄を1問に1組。10問右から左、正答は印刷しない。旧パックの印刷には触れない。
- 新しい学校画面全体の文字選択・長押しメニュー・ドラッグを抑止。Canvasではpointer captureを維持し、Apple Pencilを使った後のタッチと大きな手のひら接触を筆跡に入れない。 `touch-action:none` とスクロール抑止は記入画面に限定。実機でのApple Pencil動作はユーザーから再確認が必要。

## 2026-09-25：実テストに合わせた記入欄の再調整
- 10問テストの全文記入欄は問題文のすぐ左に寄せ、幅を狭く、高さを長くした。欄内の点線・補助線・文字ラベルをなくした。Q1も同じ配置。
- 学校テストれんしゅうの線の記入欄も問題文の左隣へ寄せた。波線は文字数別マスを廃止し、漢字と送り仮名を一続きで書ける空白の縦長欄一つに統一。答えの文字数を入力欄から推測できない。直線は従来の漢字ごとの字形判定を維持。
- 波線は一続きの筆跡として保存し、単字の自動採点に渡さず、記入後に本人／保護者が正答と比べて確認する。今週パックの既存保存領域と旧週・漢検島の進捗には変更なし。A4横プリントの配置も維持。
- 全JS/CJSの構文チェックとルートの自動テスト12本成功。`tools/school-test-browser.cjs` は新しい間隔・縦横比・波線一枠を検証するよう更新したが、この環境にはPlaywright用Chromium実行ファイルがなく起動できない。公開画面のブラウザ確認を別途行う。iPad・Apple Pencil実機は未確認。

## 2026-09-25：かなの実位置とペンの太さ
- 前回は記入枠と問題文を囲む領域の距離（14px）だけを見ていた。実際のかなは領域内で中央寄せされ、10問テストと1番の波線練習では約110px離れていた。かな列を領域の左端へ寄せ、記入枠の右に並べる。ブラウザ回帰は領域ではなくかな列そのものまでの距離を確認する。
- 共通Canvasの線幅 `13` 内部pxは、表示時の倍率が異なる縦長欄では太く見えた。描画座標を表示px基準にして直線・波線・全文いずれも3.25表示pxに揃える。直線の従来の描画太さと判定処理は維持する。

## 2026-09-26：学校テストの記入紙面を統一
- ユーザーが評価した既存「漢字プリントれんしゅう」（`app-v220-patch.js`）の中央クリーム色の紙面、上端の小さい問題番号、紙面外の操作エリアを学校テストれんしゅうと10問テストの拡大画面にも適用。両モードは `focusPaper()` の同じ紙面DOMを共有する。練習は「前の文→線の読みとCanvas→続きの文」を縦の文章の流れに置き、長い文は右から左の列へ自然に折り返す。10問テストは全文欄とかな文を中央に隣接配置。大きな横書きの読み重複・右だけ別色の問題パネル・強い黄色target背景は廃止。
- 既存の全文/targetのCanvas寸法、3.25表示pxの筆跡、Apple Pencilと手のひらのイベント処理、提出後確認・採点・保存・問題データは触れない。元のプリントれんしゅうとA4印刷のファイルも変更しない。
- 全JS/CJS構文とルートの自動テスト12本成功。`tools/school-test-browser.cjs` は新しい一体紙面・文字との距離・書き枠の四辺を1024×768と744×1133で検証する仕様へ更新。ローカルChromiumはPlaywrightの配信が壊れたZIPを返すため現環境では未実行。Pages公開後に実画面を確認し、結果を区別して報告する。iPad実機は未確認。

## 2026-09-26：今週の学校テストを一つの学習フローへ
- ホームの学校テスト入口は「今週の学校テスト」一つ。10問一覧で好きな問題から開始でき、全targetのある問題は「記入ずみ」と表示する。10問未完でも「答え合わせ」可能。
- 拡大画面は完成済みのクリーム色紙面と縦書き文の流れを維持。全文書写を廃止し、直線・波線とも線1本を一つのCanvasへまとめる。複数字の直線と漢字＋送り仮名は一括して本人／保護者が比較し、安全な単漢字だけ既存字形判定を使用する。
- 答え合わせは線単位で左に筆跡、右に縦書き正答。○・△・もう一回を下に配置し、もう一回では同じtargetの筆跡を消して書き直せる。結果画面は記入済みの問題数を表示し、問題内の全targetが○ならその問題を○とする。
- 途中の答え合わせは `schoolPracticeV236` の最終日時と問題番号だけ保存し、週別テスト回数を増やさない。10問全て記入して答え合わせした場合だけ、従来の `printTestsV230[packId]` の `best/runs/last/at` を更新する。`stats`、`okuriStats`、漢検島、旧週データには触れない。A4横印刷も変更しない。
- 回帰は `school-structure-test.cjs` と `tools/school-test-browser.cjs` を新仕様へ更新。ブラウザ実機と指定viewportの実施状況は公開確認後に記録する。
- ホームの静的HTMLだけでは旧 `app-v204-patch.js` と `app-v240-release.js` の再描画がボタンを「テストする」に戻す。現行パックの表示名・案内・ボタンは `app-v240-release.js` の `refresh()` にも反映し、旧週の表示とクリック配線を維持する。
- 公開後、`app-v204-patch.js` のスコアカード更新がさらに後からボタンだけ「テストする」に書き換えることを確認。現行パックでは同関数も「やってみる」と表示するよう修正し、旧週の得点・ボタン表示は維持した。
- 公開版の左右比較を目視し、正答が上端へ寄るのを防ぐため右欄で縦中央に配置。結果の保存・判定には触れない。


## 2026-09-26：教材62ページを今週packへ追加
- `2026-09-26-p62` を追加してcurrentへ切替。p58は `status:'past'` として保持。
- 通常練習10問とschoolTest 10問を登録。schoolTestは周辺既習漢字をかな化し、本命targetのみ直線/波線を付与。
- p62専用 `weekly-p62-test.cjs` を追加し、③「秋→あき（非target）」、⑤「書物→しょもつ」、⑨「本→ほん」、⑩周辺かな化、⑦「去る」波線を検証。
- p58既存回帰はp58 fixtureへ明示的に切り替えて継続。学校構造回帰もp58 fixtureで旧仕様保護。
- p62 current化に伴い、学校テストUIとホーム入口のp58固定判定をcurrent schoolTest pack判定へ汎用化。
- indexのdata/app cache queryを更新。関連する漢検島回帰のrelease loader期待値も新queryへ整合。
- GitHub Actions run 36202159737 が成功し、Kanji 9回帰、p58、p62、school structure、Pages deployまで成功。


## 2026-09-26：練習ノートへの常設導線
- 既存の `app-v270-practice-note.js` の「練習ノート」は、従来は書き直し・ヒント・手動OKなどadaptive needが付いた時だけ表示されていた。手書き判定の誤判定や、正解でも本人が追加練習したいケースでは到達できなかった。
- 問題記入画面のHELP直下に **「📒 たくさん練習」** を常設。判定前・不正解判定中でも、現在の漢字を直接練習ノートへ送れる。
- 正解後のreviewでも、adaptive needがなくても「たくさん練習」を表示。語の完了resultでも、needがなければその語の全漢字を自由に練習できる。
- adaptive needがある場合は従来どおり「練習ノート / 3回だけおすすめ」を優先表示する。自動判定はおすすめのきっかけには使うが、練習へのアクセス条件にはしない。
- 練習ノート本体（3回を目安、4〜6マスは任意）、保存キー `practiceNotebookV270`、Apple Pencil入力処理は維持。
- `practice-note-access-test.cjs` を追加し、判定状態に依存しない導線・新cache queryをActionsで検証。GitHub Actions run 36209708015 は成功し、Pages deployまで完了。


## 2026-09-26：複数字の「たくさん練習」修正
- 「たくさん練習」常設導線が `charIndex` 1文字だけを練習ノートへ渡していたため、2文字以上の問題でも最初の1文字しか出ない不具合を修正。
- 問題記入画面からの任意練習は、その問題の `stage.chars` 全文字をqueueへ渡す。例: 「客様」は客→様、「入学式」は入→学→式を順に練習できる。
- 正解後reviewで本人が任意に「たくさん練習」を開く場合も、その語の全漢字を対象にする。一方、adaptive needで「3回だけおすすめ」が出た場合は従来どおり該当字だけに絞る。
- `practice-note-access-test.cjs` を更新し、複数字全体queueとcache queryを回帰確認。GitHub Actions run 36210004659 は全検証・Pages deployとも成功。


## 2026-09-26：p62学校テストで既習漢字も解答対象
- 学校テストでは問題文をかな表示のままにしつつ、教材本文で漢字になっている箇所は「今週の本命」だけでなく既習漢字もすべてtargetにする方針へ変更。
- p62のtargetは計18か所。直線14・波線4。
- 追加した既習target: ② 日、③ 秋、④ 大きさ、⑤ 書物、⑨ 本、⑩ 書き・字・書く。
- 送り仮名を含む ④「おおきさ→大きさ」、⑦「さる→去る」、⑩「がき→書き」「かく→書く」は波線。その他は直線。
- 例: ②は「にゅうがくしきのひ。」とかな表示し、`にゅうがくしき→入学式` と `ひ→日` の両方を書く。③も `きょねん→去年` と `あき→秋` の両方を書く。
- A4印刷は既存 `app-v235-paper-pdf.js` が `schoolTest.lineType` をそのまま線種へ反映するため、同じ18targetを直線/波線つきで印刷。正答は表示しない。
- `weekly-p62-test.cjs` を更新し、18target、線種、既習漢字、A4上の直線14/波線4を回帰確認。GitHub Actions run 36210603010 は全検証・Pages deployとも成功。


## 2026-09-26：学校テストを1問1ページ・全記入欄同時表示へ
- 10問テストの1問画面で、同じ文のtargetを「次の線へ」で1つずつ切り替える方式を廃止。
- **1問 = 1ページ** とし、その問題に含まれる全targetの記入欄を最初から同時表示する。各かな読みの横に対応するCanvasを置く。
- 例: ②「にゅうがくしき の ひ。」では、`にゅうがくしき` 横に1欄、`ひ` 横に1欄を同じページに表示。⑩は横・書き・字・書くの4欄を同時表示。
- 各targetは従来どおり1 target = 1 Canvas。複数字・送り仮名も1つの縦長欄を維持。書きたい欄をタップすると青枠で選択され、「1画もどす」「この欄を消す」は選択中の欄だけへ作用。
- 主操作は「次の問題 →」。最後の10問目だけ「プリントにもどる」。途中でプリントへ戻ることもでき、未記入欄がある問題は一覧上で未完のまま残る。
- 答え合わせ、左右比較、書き直し、partial/full保存、A4、Apple Pencil/palm rejection、既習漢字target、旧週・漢検島データは変更なし。
- `school-structure-test.cjs` と `weekly-p62-test.cjs` を更新し、1ページ内のCanvas数がtarget数と一致すること、「次の線へ」が消えたこと、「次の問題」ナビを回帰確認。
- GitHub Actions run 36211709093 は全検証・Pages deployとも成功。
